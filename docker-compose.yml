services:
  build-emscripten:
    image: emscripten-build
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/src
    working_dir: /src
    user: "${UID:-1000}:${GID:-1000}"
    environment:
      - BUILD_EMSCRIPTEN=1
